version: '3'
services:
  paw:
    build:
      context: .
      dockerfile: Dockerfile-paw
    ports:
     - "8081:8080"
     - "8901"
    volumes:
     - ./data-config:/data/config
  jenkins:
    build: vendor/jenkins
    ports:
     - "8080:8080"
  nginx:
    build: vendor/nginx
    ports:
     - "80:80"
  redis:
    image: "redis:alpine"
    ports:
    - "6379"
